import { motion } from 'motion/react'
const METHODS = [{ title: 'Evidence-grounded generation', body: 'TrustStack constrains answers to retrieved evidence and marks unsupported responses as insufficient rather than confident.' }, { title: 'Multi-signal trust scoring', body: 'The scoring layer combines retrieval relevance, evidence quantity, citation behavior, and consistency signals into a single trust view.' }, { title: 'Risk-first reporting', body: 'Instead of showing only a model answer, the console highlights where human review is required and why the system is uncertain.' }]
export function MethodologySection() { return (<div className="methodology-panel"><div className="section-heading"><div><div className="eyebrow">About / Methodology</div><h2>The interface is cinematic, but the evaluation story stays academically credible.</h2></div><p>This page anchors the demo back to the project report: architecture, methodology, limitations, and why the system is materially different from a standard chatbot wrapper.</p></div><div className="method-grid">{METHODS.map((item, index) => <motion.div className="method-card panel panel--glass" key={item.title} initial={{ opacity: 0, y: 18 }} whileInView={{ opacity: 1, y: 0 }} transition={{ delay: index * 0.08, duration: 0.35 }} viewport={{ once: true, amount: 0.3 }}><div className="method-index">{String(index + 1).padStart(2, '0')}</div><h3>{item.title}</h3><p>{item.body}</p></motion.div>)}</div></div>) }
