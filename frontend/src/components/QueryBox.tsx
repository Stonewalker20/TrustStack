import { useState } from 'react'
export function QueryBox({ onSubmit, loading }: { onSubmit: (question: string) => void; loading: boolean }) { const [question, setQuestion] = useState(''); return (<div className="panel panel--glass stack"><div className="panel-header"><div><div className="eyebrow">Run Evaluation</div><h2>Probe the model with a grounded question</h2></div></div><textarea className="textarea" placeholder="Ask a grounded question over your indexed documents..." value={question} onChange={(e) => setQuestion(e.target.value)} /><div className="query-actions"><button className="primary primary--glow" disabled={loading || !question.trim()} onClick={() => onSubmit(question)}>{loading ? 'Evaluating…' : 'Run Query'}</button><div className="muted">Best demo pattern: ask one clearly supported question, then one weakly supported question.</div></div></div>) }
